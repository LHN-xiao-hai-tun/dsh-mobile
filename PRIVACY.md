# 隐私政策（Privacy Policy）

**DSH Mobile 是一个本地客户端**，用于连接**你自己**运行的
[DeepSeek Harness](https://github.com/deepseek-ai/deepseek-harness)。
它**不含 DSH 本体**、**不预置任何服务器地址**、**不采集任何数据**。

---

## 一、我们不收集

- ❌ 不收集个人身份信息
- ❌ 不收集设备标识符
- ❌ 不做使用统计 / 埋点
- ❌ 不集成广告 SDK
- ❌ 不集成第三方分析 SDK
- ❌ 不做崩溃上报（没有崩溃收集服务；系统 logcat 由你在设备上自行导出）
- ❌ 不访问通讯录、短信、相册、精确位置

App 自身**唯一的网络行为**，是访问**你填写的那个地址**。

---

## 二、本机保存了什么

以下内容**只存在本机** —— App 私有目录下的 `SharedPreferences`
（文件名 `dsh`，`MODE_PRIVATE`，仅本 App 可读）：

| 数据 | 键 | 说明 |
|---|---|---|
| 服务器地址 | `url` | 你填的那个 |
| 连接历史 | `history` | 最近 **5** 条（去重，最近在前） |
| 快捷指令模板 | `tpl_0` ~ `tpl_2` | 3 条可编辑文字 |
| 浮动按钮位置 | `fab_x` / `fab_y` | 存的是「可移动范围的比例」，旋转屏幕后仍合理 |

**没有保存**：DSH 的 PIN / 密码（App 不存 PIN —— PIN 在网页里输入，之后由 Cookie 承载登录态）、任何密钥。

### 🔐 加密存放（v1.3.0 起）

上面这些**敏感项已在设备上加密保存**：

- 算法 **AES-256-GCM**，密钥由 **Android Keystore** 生成并保管 —— **不出设备**，应用卸载即销毁
- 落盘格式带 `enc1:` 版本前缀，将来换算法可平滑再迁
- 旧版本留下的**明文值仍能正常读出**，**下一次写入时自动变成密文**
  ⇒ **升级不会丢地址**（惰性迁移，没有迁移弹窗、没有迁移脚本）

**没有加密的**：浮动按钮位置（`fab_x` / `fab_y`）—— 它只是一个 UI 比例坐标，加密它没有安全收益。

> 本 App **刻意不引入** `androidx.security:security-crypto`：它已被 Google 废弃，
> 且会把 Tink 依赖拖进来，和本项目「约 3 MB」的定位冲突。
> 加密由项目内的 `SecretStore.java` 用标准 API 实现，源码可审计。

### 仍然要注意的

- App 私有目录在**未 root** 的设备上，其他 App 读不到；
- **设备被 root 或存在恶意软件**时，加密也挡不住（能用密钥就能解密）；
- ⇒ **不要把 PIN 之类的敏感凭据保存在设备上** —— 本 App 也确实不保存 PIN；
- 本 App **不参与 Android 云备份**（清单里 `allowBackup="false"`）—— 换机请重新填地址。

### 怎么清除

- 在「设置」页**清空地址**即可清除对应数据；
- **卸载 App** 会清除全部本地数据。

---

## 三、网络通信

- App **只与你配置的 DSH 地址通信** —— 不向任何第三方服务器发送数据。
- **登录态**通过 **Cookie** 保存在 WebView 中（`CookieManager`），用于免去反复登录；Cookie 只对该地址生效。
- **mDNS / NSD 发现**（v1.2.7 起）只在**局域网内**监听，用于发现 DSH 服务，**不向互联网发送任何内容**。
- **子网扫描兜底**：对同一子网做 **TCP 连接探测**（连上即断，**不发送任何应用层数据**），
  结果只留在内存，**不落盘、不上传**。
- 「关于 → 检查更新」**只在你点它时**才请求一次 GitHub 的 `releases/latest`（v1.3.9 起，C1）——
  用来告诉你有没有新版本。**不会自动检查**（没有定时器、没有后台任务）、**不会自动下载**：
  发现有新版只给一个「打开下载页」按钮，交给系统浏览器。

---

## 四、权限说明

App 在 `AndroidManifest.xml` 中声明 **5 个权限**：**基础 3 条**（一直都需要）

| 权限 | 用途 |
|---|---|
| `INTERNET` | 连接你配置的 DSH 服务 |
| `ACCESS_NETWORK_STATE` | 判断网络是否可用（网络恢复时自动重连，v1.2.5） |
| `CHANGE_WIFI_MULTICAST_STATE` | 获取组播锁（`MulticastLock`），用于局域网 mDNS/NSD 发现（v1.2.7） |

以及 **2 条「要用才开」的可选功能权限**（v1.3.9 起）：

| 权限 | 对应功能 | 默认 | 什么时候才会申请 |
|---|---|---|---|
| `POST_NOTIFICATIONS` | 下载完成 / 失败时在应用外提示 | **关** | 你在设置里打开「下载完成时通知我」时（Android 13+ 才有这条运行时权限；8~12 不需要申请） |
| `CAMERA` | 网页拍照上传（页面里带 `capture` 的文件输入框） | **关** | 你在设置里打开「网页拍照上传」时 |

> ⚠️ **声明 ≠ 申请**：这两条一直关着时，App **不会**调用权限申请、系统**不会**弹任何窗，
> 对应功能也**不启用**（网页里的拍照输入框会像老版本一样走系统文件选择器）。

App **默认不申请**定位、麦克风、通讯录、存储、电话、蓝牙等权限。

### 权限口径（2026-09-23 明确）

> 用户 2026-09-23 定：**「选择留给用户，他们想用就直接允许」** ⇒ 权限不是"一律不做"，
> 而是**逐功能、可选、默认关闭**。本节把口径写清楚，供后续版本引用。

三条规则：

1. **默认不申请**：所有会碰用户数据或需运行时授权的权限，**默认不写进清单、不弹窗**。
2. **按功能开启**：某个功能确实需要权限时 ⇒ 该功能**默认关闭**，并且在**用户主动打开它时**才
   申请对应权限（系统弹窗由系统出，解释文案由 App 先讲清楚）。
3. **一功能一权限、能不用就不用**：能用系统选择器（SAF）解决的**不申请存储权限**；
   能用前台服务/用户可见 UI 解决的**不申请通知权限**。若某功能只有拿到权限才能做，才申请。

**当前状态（v1.3.9）**：可选功能已落地 2 项 —— **下载完成通知**与**网页拍照上传**，
对应 2 条权限已写进清单，但**默认关闭**（见 §四 的「要用才开」表）。

| 功能 | 权限 | 用途 | 默认 | 打开开关时会发生什么 |
|---|---|---|---|---|
| 下载完成 / 失败时通知 | `POST_NOTIFICATIONS`（Android 13+ 运行时权限） | 在应用外提示下载结果 | **关** | Android 13+ 弹一次系统授权窗；8~12 无需申请（直接可用）。**不给权限 ⇒ 开关自动退回关闭** |
| 网页拍照上传（`<input capture>`） | `CAMERA` | 让网页能直接调相机拍一张 | **关** | 弹一次系统授权窗。**不给权限 ⇒ 开关自动退回关闭** |
| 用系统下载器写公共下载目录 | `WRITE_EXTERNAL_STORAGE`（仅 API 26–28） | — | ⛔ **不做**：已用 SAF 替代，无需此权限 |
| 读 WiFi SSID 做 mDNS 加速 | 定位 / 近场权限 | — | ⛔ **不做**：刻意避开（见下） |

> 📌 **通知的口径补充**：本 App **不用前台服务**，因此下载**结果**通知只在进程还活着时发出
> （下载本来就在本进程里跑）。若系统在长下载途中回收了进程，那条结果通知就发不出来 —— 这是已知边界，
> 如实写在这里。为此**不**引入 `FOREGROUND_SERVICE` 系列权限。

⛔ **仍然刻意不做的两条**（不是因为"要权限"，而是**收益不值**）：
- 用「读 WiFi SSID」改进 mDNS 发现（要定位权限，而现有 NSD + 子网扫描已够用）；
- 用系统下载器写公共目录（要存储权限，而 SAF 已经完全够用且更可控）。

> **关于合并清单里多出的那一条（如实说明）**：
> 上面是**源码清单**里的声明。打包后的**合并清单**里还会多出一条
> `app.dshmobile.DYNAMIC_RECEIVER_NOT_EXPORTED_PERMISSION`
> —— 这是 **androidx 自动生成**的**应用自有签名级权限**（用于限制广播接收器不被外部 App 调用），
> **不涉及任何系统数据或用户信息**，也不会向用户暴露任何权限授予界面。
> 用 `aapt dump badging` 审计时会看到它，特此说明。

> 补充：mDNS 发现**本可**用「读 WiFi SSID」的方式做，但那需要**定位/近场权限** ——
> 本项目决定**不为它申请**（见 `AndroidManifest.xml` 内注释）：
> 不是"永远不许有权限"，而是**这一项收益不值那一条权限**。

### 网页内的权限请求

若 DSH 网页里请求麦克风 / 摄像头（例如语音输入），App 会在 **WebView 层按来源放行**
（`MainActivity.onPermissionRequest`：**只对你在设置里配置的那个主机**放行音频/视频采集，其余来源拒绝），
目的是让语音输入可用。

是否真正授予仍由 **Android 系统**决定；App **未**在清单中申请相机 / 麦克风权限。
（若将来做「网页拍照上传」，会按上面 §权限口径 的规则：默认关闭 + 用户打开开关时才申请 `CAMERA`。）

---

## 五、明文流量说明

本 App **允许明文 HTTP**，因为它的主用途就是**局域网内的 `http://` 连接** ——
`dsh-pocket` 的局域网入口默认就是 `http://<电脑IP>:3081`。

明文策略有**单一真源**：`res/xml/network_security_config.xml`
（全局放行明文 + **信任锚只列系统 CA**，即不接受用户手动安装的 CA）；按地址的放行判断在代码层（`NetPolicy`）。

实际行为（v1.3.0 起）：

| 你要连的地址 | App 的行为 |
|---|---|
| `https://…` | 直接连，不打扰；证书走 TOFU（首次确认指纹） |
| `http://` + **私有网段**（10./172.16-31./192.168./127./.local 等） | 告知「这是明文」，**首次确认一次**，之后不再打扰 |
| `http://` + **公网地址** | **默认拦下**并说明风险，要你明确接受才继续 |

> ⚠️ 明文连接的内容**仍可被同一网络下的他人窃听**（这是明文本身的性质，App 挡不住，只能提醒）。
> 请优先使用 **HTTPS** 或**组网**（Tailscale / ZeroTier / WireGuard），
> 详见 [`docs/SECURITY_DEPLOY.md`](docs/SECURITY_DEPLOY.md)。

---

## 六、儿童隐私

本 App 不面向儿童，也不收集任何用户数据。

---

## 七、政策变更

本政策如有变更，会在本仓库与本项目的 Release Notes 中说明。

---

## 八、联系

- **隐私相关问题**：请到
  [Issues](https://github.com/LHN-xiao-hai-tun/dsh-mobile/issues) 提出
  —— **不要贴真实地址、PIN 或任何凭据**。
- **安全问题**：请走 [`SECURITY.md`](SECURITY.md)（**不要公开披露细节**）。
